package com.pedrotavares.paidmedia.utils.dateCountdown{	import flash.utils.Timer;	import flash.events.TimerEvent;	public class DateCountdown	{		/*		THIS IS INCOMPLETE AND DOES NOT WORK AT THE MOMENT		TO DO: GET THIS WORKING		*/				public var targetDate:Date;		private var today:Date;		public var timeToToday:Number;		private var timer:Timer;		public function DateCountdown(targetDate:Date)		{			// Make targetDate global			this.targetDate = targetDate;						// Set up checking the time every second			timer = new Timer(1000);			timer.addEventListener(TimerEvent.TIMER, updateTimer);			// Start counting the seconds			timer.start();		}				private function updateTimer(e:TimerEvent):Number		{			// Get today's date			today = new Date();			// Subtract today's date from target date to find out how much time is remaining			 timeToToday = targetDate.getTime() - today.getTime();			// Check if today's date is past target date			if ( timeToToday <= 0 ) {				//trace("Don't count anymore");				// If it is, countdown is zero				timeToToday = 0;				// Stop the timer				//timer.removeEventListener(TimerEvent.TIMER, updateTimer);			}			//trace(timeToToday);			// Dispatch a notice that the time has updated						return(timeToToday)			//trace("Today is ", today, " Target Date is ", targetDate);		}	}}